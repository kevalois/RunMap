// == Import : npm
import React from 'react';
import {Typeahead, Fragment, FormGroup, Control } from 'react-bootstrap-typeahead';


// == Composant
const AutoComplete = () => (
  <>
    {/* <Fragment>
      <Typeahead
        labelKey="name"
        // multiple={multiple}
        // options={options}
        placeholder="Choose a state..."
      />
      <FormGroup>
        <Control
          // checked={multiple}
          // onChange={(e) => this.setState({multiple: e.target.checked})}
          type="checkbox"
        >
          Multi-Select
        </Control>
      </FormGroup>
    </Fragment> */}
  </>
);

// == Export
export default AutoComplete;
